#!/usr/bin/env bash

function initializeEnvironment {
    export UID=${UID} 2>/dev/null
}


function eslint {
    docker-compose -f docker-compose-dev.yml run build ./node_modules/.bin/eslint $@
}
function main {
    initializeEnvironment
    eslint $@
}

main $@