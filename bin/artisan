#!/usr/bin/env bash

function initializeEnvironment {
    export UID=${UID} 2>/dev/null
}


function artisan {
    docker-compose run build ./artisan $@
}
function main {
    initializeEnvironment
    artisan $@
}

main $@